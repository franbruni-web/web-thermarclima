{
  "name": "thermarclima-web",
  "version": "1.0.0",
  "description": "Sitio web de Thermarclima",
  "scripts": {
    "dev": "tailwindcss -i ./input.css -o ./css/style.css --watch",
    "build": "tailwindcss -i ./input.css -o ./css/style.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}